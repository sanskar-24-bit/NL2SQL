# 🧠 Text-to-SQL with Google Gemini

A natural language to SQL query converter built with **Google Gemini AI** and **Streamlit**. Ask questions in plain English and get real-time results from a SQLite database — no SQL knowledge required.

---

## 📌 Features

- Converts natural language questions into SQL queries using Gemini AI
- Executes the generated query on a local SQLite database
- Displays query results instantly via a clean Streamlit UI
- Pre-loaded student database with 90+ records across 15+ classes

---

## 🗂️ Project Structure

```
text-to-sql-gemini/
│
├── app.py               # Main Streamlit application
├── sql.py               # Script to create and populate the SQLite database
├── student.db           # SQLite database (auto-generated by sql.py)
├── requirements.txt     # Python dependencies
├── .env                 # Environment variables (not committed to GitHub)
├── .env.example         # Sample environment file
└── README.md
```

---

## 🛠️ Tech Stack

| Layer    | Technology                   |
| -------- | ---------------------------- |
| Frontend | Streamlit                    |
| AI Model | Google Gemini (`gemini-pro`) |
| Database | SQLite3                      |
| Language | Python 3.8+                  |
| Config   | python-dotenv                |

---

## 🗃️ Database Schema

The `STUDENT` table has the following columns:

| Column    | Type        | Description         |
| --------- | ----------- | ------------------- |
| `NAME`    | VARCHAR(25) | Student name        |
| `CLASS`   | VARCHAR(25) | Course/class name   |
| `SECTION` | VARCHAR(25) | Section (A / B / C) |
| `MARKS`   | INT         | Marks obtained      |

**Sample classes include:** Data Science, Machine Learning, DEVOPS, Cyber Security, Cloud Computing, Artificial Intelligence, Blockchain, NLP, IoT, and more.

---

## ⚙️ Setup & Installation

### 1. Clone the Repository

```bash
git clone https://github.com/your-username/text-to-sql-gemini.git
cd text-to-sql-gemini
```

### 2. Create a Virtual Environment (Recommended)

```bash
python -m venv venv
source venv/bin/activate        # On Windows: venv\Scripts\activate
```

### 3. Install Dependencies

```bash
pip install -r requirements.txt
```

### 4. Set Up Environment Variables

Create a `.env` file in the root directory:

```env
GOOGLE_API_KEY="your_google_api_key_here"
```

> 🔑 Get your API key from [Google AI Studio](https://aistudio.google.com/app/apikey)

### 5. Initialize the Database

Run this once to create and populate the `student.db` SQLite database:

```bash
python sql.py
```

### 6. Run the Application

```bash
streamlit run app.py
```

The app will open in your browser at `http://localhost:8501`

---

## 💡 Example Queries You Can Ask

| Natural Language Question                      | Generated SQL                                                    |
| ---------------------------------------------- | ---------------------------------------------------------------- |
| How many students are there?                   | `SELECT COUNT(*) FROM STUDENT;`                                  |
| Show all students in Data Science              | `SELECT * FROM STUDENT WHERE CLASS="Data Science";`              |
| Who scored more than 90 marks?                 | `SELECT * FROM STUDENT WHERE MARKS > 90;`                        |
| List all students in Section A                 | `SELECT * FROM STUDENT WHERE SECTION="A";`                       |
| What is the average marks in Machine Learning? | `SELECT AVG(MARKS) FROM STUDENT WHERE CLASS="Machine Learning";` |

---

## 🔐 Environment Variables

| Variable         | Description                |
| ---------------- | -------------------------- |
| `GOOGLE_API_KEY` | Your Google Gemini API key |
